---
title: "アクセスとプライバシー"
date: 2026-08-20
description: "Everdisk の共有を安全に保ちましょう。ログインとパスワードでアクセスを保護し、SMB3 (AES) で SMB 接続を暗号化し、ファイルの編集で接続中のデバイスがアップロード、名前の変更、削除をできるかどうかを制御し、見知らぬデバイスをブロックし、ゴミ箱か完全削除かを選び、なぜすべてがローカルネットワーク内にとどまるのかを理解できます。"
keywords: ["Everdisk パスワード保護", "SMB 暗号化", "SMB3 AES 暗号化", "ファイルの編集トグル", "デバイスをブロック", "ブロックしたデバイス", "ファイルを完全に削除", "ローカルネットワークのみ", "プライベートなファイル共有", "DLNA パスワードなし", "ネットワークの安全性"]
tags: ["everdisk", "guide", "access", "privacy", "security"]
readingTime: 8
---


Everdisk はあなたのファイルを自分のネットワーク内に保ち、誰がアクセスでき、何ができるかをシンプルに管理できるようにします。これらの管理項目は **設定 → 共有 → アクセス** にあるほか、ファイルマネージャーにも関連するいくつかの設定があります。

## ログインとパスワードでアクセスを保護する

初期設定では、あなたのアドレスを知っている同じネットワーク上の誰でも、共有ファイルを開けます。サインインを必須にするには:

1. **設定 → 共有 → アクセス** に移動します。
2. **ログイン** と **パスワード** を入力します。
3. これで **ブラウザ (HTTP)**、**コンピュータ (WebDAV)**、**コンピュータ（詳細） (SMB)**、**その他のアプリやデバイス (FTP)** の各接続は、ファイルを表示する前にその情報を求めるようになります。

オープンアクセスにするには、両方のフィールドを空欄のままにします。パスワードはデバイスのキーチェーンに安全に保存されます。

> **DLNA は常にオープンです。** TV とメディアセンター (DLNA) 接続はパスワードで保護できないため、オンにすると同じ Wi-Fi 上のどのデバイスでも共有メディアを閲覧できます。保護された接続だけにしたい場合はオフにし、信頼できるネットワークでのみ共有しましょう。

## SMB 接続を暗号化する (SMB3 / AES)

ログインとパスワードは **誰が** 接続できるかを制御しますが、ほとんどの接続ではデータそのものは平文のまま送られます。**SMB は Everdisk が暗号化できる唯一の接続で**、すべての転送を暗号化して、同じネットワーク上の他の人が読めないようにします。

オンにするには:

1. 上記のとおり **ログイン** と **パスワード** を設定します。暗号化された接続は匿名にできません。
2. **設定 → 共有** に移動し、**SMB 暗号化を必須にする** をオンにします。
3. 変更を反映させるため、共有を **一度 停止 してから再び スタート** します。

これで、すべての SMB 転送が **SMB3 暗号化 (AES)** で保護されます。接続するデバイスは SMB3 に対応している必要があります。最新の Mac の Finder、または **Windows 10 以降** です。完全には信頼できない Wi-Fi では最適な選択です。SMB 暗号化は Premium 機能です。

## 編集を許可またはブロックする (ファイルの編集)

**ファイルの編集** トグルは、接続中のデバイスがあなたのファイルを見るだけか、変更もできるかを制御します。

- **オン** (初期設定): 接続中のデバイスはあなたの共有ファイルを **アップロード、名前の変更、削除** できます。あなたのデバイスは本物の双方向ネットワークドライブのように動作します。
- **オフ**: あなたの共有ファイルは **読み取り専用** になります。他の人は閲覧とダウンロードはできますが、何も追加・変更できません。

オンにすると、他の人があなたのファイルを変更できるようになるため、短い警告が表示されます。オンの間は **重要** バッジが付きます。

## デバイスをブロックする

見覚えのないデバイスがある場合:

1. 共有画面の **接続中のデバイス** でそれを見つけます。
2. そのその他のアクションボタンをタップし、**このデバイスをブロック** を選びます。

ブロックしたデバイスは **設定 → 共有 → アクセス → ブロックしたデバイス** に一覧表示され、そこで 1 つを **ブロック解除** したり **すべてブロック解除** したりできます。ブロックは、デバイスのネットワークアドレスが変わっても追従します (ブラウザ、コンピューター、テレビの接続について)。

## ゴミ箱と完全削除

ファイルが削除されると - ファイルマネージャーであなたが削除した場合も、接続中のデバイスが削除した場合も - 通常は復元可能な **ゴミ箱** に入るので、元に戻せます。

復元なしでファイルを即座に取り除きたい場合は、**設定 → ファイルマネージャ → ファイルを削除中** で **ファイルを完全に削除** をオンにします。これは初期設定でオフです。**デバイス上のファイルマネージャー** と **ネットワーク経由の削除** に影響しますが、システムの写真ライブラリや音楽ライブラリの削除の扱いは変わりません。

## すべてがローカルにとどまる

Everdisk が共有するのは **ローカルネットワーク** 経由のみです。インターネットには何もアップロードされず、間にクラウドアカウントが入ることもありません。知っておくとよいことがいくつかあります。

- Everdisk は、近くのデバイスから見つけられるように iOS の **ローカルネットワーク** 権限を必要とします。その権限がオフの場合、iOS の設定アプリで再びオンにする方法を案内します。
- プライバシーを最大限に保つには、信頼できる **自宅または非公開の Wi-Fi** ネットワークにいるときだけ共有し、公共の Wi-Fi では注意しましょう。ログインとパスワードは役立ちますが、信頼できるネットワークの代わりにはなりません。
- **最もプライベートな方法は、Mac への USB ケーブル接続** です。データはケーブルを直接通り、ルーターやインターネットに触れることがありません。[デバイスを接続する](/docs/guide/everdisk/everdisk-guide-connect)を参照してください。

## 次のステップ

- [共有](/docs/guide/everdisk/everdisk-guide-sharing) - 共有するものを選び、共有を開始します。
- [設定](/docs/guide/everdisk/everdisk-guide-settings) - アクセスとファイルマネージャーのすべての設定を 1 か所で確認できます。
